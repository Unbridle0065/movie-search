services:
  movie-search:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - OMDB_API_KEY=${OMDB_API_KEY}
      - AUTH_USER=${AUTH_USER}
      - AUTH_PASS=${AUTH_PASS}
      - SESSION_SECRET=${SESSION_SECRET}
    restart: unless-stopped
